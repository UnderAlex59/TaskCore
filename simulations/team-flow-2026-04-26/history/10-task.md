# Экспорт и архив согласованных заявок

            - Task ID: 3c4fdb74-c6d2-4119-b8b3-73c25261d2de
            - Теги: security, integration, reports
            - Дата создания: 2026-04-26T09:33:47.047049Z

            ## Исходное описание

            # Экспорт и архив согласованных заявок

- Теги: security, integration, reports

## Стартовая редакция

Нужно описать экспорт и архивирование согласованных заявок после завершения процесса. Пользователь с административными правами должен иметь возможность выгрузить карточку заявки, историю статусов, список согласующих, вложения и связанные интеграционные статусы, а затем перевести запись в архив без потери возможности для последующего аудита.

Требование должно учитывать срок хранения, требования к маскированию персональных данных в отчётах и связь с уже сохранёнными batch id массовых операций и интеграционными журналами. Для магистерского сценария важно, чтобы здесь можно было обсудить спорное предложение команды и показать, как аналитик принимает или отклоняет change proposal.

Критерии приёмки:
1. Экспорт включает историю статусов, согласующих, вложения и интеграционные события.
2. Архивирование не удаляет данные, нужные для аудита и расследований.
3. В отчётах маскируются поля, которые не нужны для просмотра руководителю.

## Редакция после доработки

Нужно описать экспорт и архивирование согласованных заявок после завершения процесса. Пользователь с административными правами выгружает карточку заявки, историю статусов, список согласующих, вложения, связанные batch id массовых операций и интеграционные журналы, а затем переводит запись в архивный режим без потери следов для аудита.

Требование должно отдельно фиксировать правила хранения и маскирования данных. В архиве сохраняются все технические идентификаторы и история действий, но в управленческом отчёте скрываются персональные поля, которые не нужны руководителю. Если заявка участвовала в массовом изменении маршрута или повторной интеграционной отправке, экспорт должен включать эти связи как часть полного следа по заявке.

Критерии приёмки:
1. Экспорт включает статусы, согласующих, вложения, batch id и интеграционные события.
2. Архивирование не удаляет данные, нужные для аудита и регуляторных проверок.
3. Отчёт для руководителя маскирует поля, не нужные для принятия решения.

            ## Хронология

            - 2026-04-26T14:33:47+05:00: Создана задача в статусе `draft`.
- 2026-04-26T14:33:47+05:00: Для первого живого прогона в задачу сразу добавлено стандартизированное приложение с критериями.
- 2026-04-26T14:34:57+05:00: Автопроверка вернула `needs_rework`, задача будет усилена и перевалидирована.
- 2026-04-26T14:36:19+05:00: После `after-strengthening` attempt 1 аналитик уточнил критерии и граничные сценарии.

            ## Ключевые сообщения чата

            - Пока без сообщений.

            ## Результаты валидации

            - 2026-04-26T14:34:57+05:00: `initial` -> `needs_rework`; issues: Не определён формат экспорта (CSV, JSON, XML, PDF) и способ передачи вложений (бинарные данные, ссылки, архив); Не определён API-эндпоинт для инициирования экспорта и перевода в архив. Scope заявляет проверку через API, но эндпоинты не описаны; Критерии приёмки не содержат конкретных входных данных, ожидаемых результатов и разрешённых актёров для каждого шага, хотя требование 1 машинных критериев это декларирует; Противоречие: Scope исключает UI из критериев приёмки, но описание содержит действия пользователя ('выгружает', 'переводит'), подразумевающие интерфейс; Не определён критерий 'завершение процесса' — какие статусы заявки считаются завершёнными для архивирования; Не определён формат маскирования персональных данных: примеры e-mail (***@domain.com или user***@***.com), персональные идентификаторы (какие именно — ИНН, СНИЛС?); Не определён механизм блокировки автоудаления при 'открытом расследовании': как устанавливается флаг расследования, кто может его установить, через какой API; Не определены роли и права доступа: какие именно административные права требуются, может ли руководитель сам запрашивать управленческий отчёт; Не определены требования к производительности: допустимое время экспорта, обработка больших объёмов вложений; Не определена схема БД или структуры данных для проверки требований (таблицы аудита, архива, интеграционных журналов); questions: Какой формат файла экспорта ожидается (JSON, CSV, XML, PDF)?; Какие именно поля входят в 'карточку заявки' для экспорта?; Какой API-эндпоинт инициирует экспорт согласованных заявок?; Какой API-эндпоинт переводит заявку в архивный режим?; Какие статусы заявки считаются 'завершёнными' для возможности архивирования?; Какие персональные идентификаторы подлежат маскированию (ИНН, СНИЛС, паспорт)?; Какой точный формат маскирования: user***@***.com или ***@domain.com?; Как определяется 'открытое расследование' — через какой атрибут заявки или отдельный объект?; Какие роли могут: (а) инициировать экспорт, (б) запрашивать управленческий отчёт, (в) устанавливать блокировку расследования?; Как экспортируются вложения — как бинарные данные, как ссылки, или в составе ZIP-архива?; Какие именно поля входят в 'интеграционные журналы' для экспорта?; Как связаны batch_id с заявками — через отдельную таблицу или поле в заявке?; Какова структура таблицы аудита для проверки требований к event_id, entity_type, entity_id?; Какой API возвращает данные для управленческого отчёта с маскированием?; Предусмотрена ли версионность экспорта — сохраняется ли история экспортов для одной заявки?
- 2026-04-26T14:36:16+05:00: `after-strengthening-1` -> `needs_rework`; issues: Описание задачи фокусируется на экспорте и архивировании согласованных заявок, но машиночитаемая спецификация описывает интеграционные события (внешний payload, идемпотентность, delivery_status) — это отдельная функциональность, не связанная напрямую с экспортом/архивом. Требуется либо объединить эти требования в единую логику, либо разделить на две независимые задачи.; Критерий приёмки требует проверки через API, но не определены конкретные endpoints для операций экспорта и архивирования. Отсутствуют: URL, HTTP-методы, формат запросов/ответов, коды ошибок.; Не определено техническое решение для 'архивного режима': это статус заявки, отдельный флаг в БД, перенос в архивную таблицу илиsoft delete? Без этого невозможно проверить корректность реализации.; Требование 'маскируются e-mail и персональные идентификаторы, кроме последних 4 символов' неполно: какие именно поля персональных данных? Как маскируется email — только домен или часть до @? Какие ещё поля могут содержать ПДн?; Требование 'при открытом расследовании автоудаление запрещено' не содержит механизма: как система определяет наличие расследования? Какой актор создаёт расследование? Есть ли API для установки/снятия флага расследования?; Не определён формат экспорта (PDF, Excel, JSON, CSV). Для автоматической валидации через API критически важно знать структуру ответа.; Требование 'экспорт должен включать эти связи как часть полного следа по заявке' не детализирует: как именно представляются связи с batch_id — список ID, вложенный объект, отдельная секция?; Критерий приёмки требует 'граничные случаи и ошибки перечислены явно', но в спецификации отсутствуют: что происходит при экспорте пустой заявки, при отсутствии вложений, при ошибке доступа к интеграционным журналам, при параллельном архивировании одной заявки двумя админами.; questions: Какова связь между интеграционными событиями (payload, correlation_id, delivery_status) и задачей экспорта/архивирования? Это часть одного workflow или отдельная функциональность?; Какие именно API endpoints должны быть реализованы для экспорта (GET /archive/export/{id}?) и архивирования (POST /archive/{id} илиPATCH /requests/{id}/archive)?; Как технически реализуется 'архивный режим' — статус заявки, флаг is_archived, отдельная таблица archive_requests?; Какие конкретно поля подлежат маскированию в управленческом отчёте (полный список)?; Как маскируется email — например, 'ivanov@example.com' -> '****@example.com' или 'i****@example.com'?; Как система фиксирует 'открытое расследование' — это статус заявки, отдельная сущность Investigation, флаг в БД?; Какой формат экспорта ожидается — JSON, XML, PDF, Excel?; Кто может инициировать расследование и как оно влияет на срок хранения?; Должен ли экспорт включать данные из интеграционных журналов (external payload, delivery_status) или только внутренние данные заявки?; Как обрабатывается случай, когда заявка участвовала в нескольких массовых операциях — все batch_id в одном поле или связанная структура?

            ## Сохранённые backlog-вопросы

            - QA backlog для этой задачи не пополнялся.

            ## Повторное использование backlog-вопросов

            - Повторно использованных backlog-вопросов не зафиксировано.

            ## Change Proposals

            - Proposal-событий пока нет.

            ## Вложения

            - Вложений нет.

            ## Финальный статус

            - ещё не завершена

            ## Вывод для магистерской

            - Сценарий показал базовый lifecycle задачи и взаимодействие команды.
