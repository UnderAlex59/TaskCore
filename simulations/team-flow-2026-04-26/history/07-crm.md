# Синхронизация статусов с внешней CRM

            - Task ID: e8c88e2e-66e0-413f-8866-8feee94973d2
            - Теги: integration, reports, audit
            - Дата создания: 2026-04-26T08:58:01.809716Z

            ## Исходное описание

            # Синхронизация статусов с внешней CRM

- Теги: integration, reports, audit

## Стартовая редакция

Нужно описать интеграцию со внешней CRM, чтобы после ключевых изменений статуса заявки система могла публиковать событие наружу. Интеграция должна передавать идентификатор заявки, внешний идентификатор инициатора, статус, сумму и дату последнего изменения, а также уметь безопасно переживать временную недоступность CRM.

Требование должно объяснить, когда событие считается успешно доставленным, что делать при ошибке на стороне CRM и какие записи нужны для последующего аудита и отчётов. Для последовательного backlog важно, чтобы здесь появились вопросы о повторной отправке и о поведении при несовпадении данных между внутренним маршрутом и внешней системой.

Критерии приёмки:
1. Для каждого статуса, публикуемого в CRM, определён состав события.
2. Ошибка внешней системы не приводит к потере внутреннего перехода статуса.
3. Повторные отправки фиксируются в аудите и доступны для отчётности.

## Редакция после доработки

Нужно описать интеграцию со внешней CRM, чтобы после ключевых изменений статуса заявки система публиковала событие наружу. В событие входят идентификатор заявки, внешний идентификатор инициатора, статус, сумма, код подразделения, дата последнего изменения и ссылка на внутренний журнал аудита.

Требование должно зафиксировать модель повторной отправки: если CRM недоступна или вернула ошибку бизнес-валидации, внутренний переход статуса остаётся завершённым, а публикация получает отдельный статус доставки, причину ошибки и время следующей попытки. Для магистерской проверки важно, чтобы история повторных отправок была доступна в отчётности и чтобы вопросы команды о восстановлении после сбоя можно было потом увидеть в validation следующих задач.

Критерии приёмки:
1. Внутренний статус заявки не откатывается из-за ошибки внешней CRM.
2. Каждая попытка доставки содержит код результата, время и краткое описание ошибки.
3. Повторная отправка одной и той же заявки не должна терять ссылку на исходный переход статуса.

            ## Хронология

            - 2026-04-26T13:58:01+05:00: Создана задача в статусе `draft`.
- 2026-04-26T13:58:01+05:00: Для первого живого прогона в задачу сразу добавлено стандартизированное приложение с критериями.
- 2026-04-26T13:59:22+05:00: Автопроверка вернула `needs_rework`, задача будет усилена и перевалидирована.
- 2026-04-26T14:06:07+05:00: После `after-strengthening` attempt 1 аналитик уточнил критерии и граничные сценарии.
- 2026-04-26T14:10:58+05:00: После `after-strengthening` attempt 2 аналитик уточнил критерии и граничные сценарии.
- 2026-04-26T14:14:38+05:00: После `after-strengthening` attempt 3 аналитик уточнил критерии и граничные сценарии.

            ## Ключевые сообщения чата

            - Пока без сообщений.

            ## Результаты валидации

            - 2026-04-26T13:59:22+05:00: `initial` -> `needs_rework`; issues: В критерии приёмки пункт 1 утверждает: «Для каждого шага описаны входные данные, разрешённый актор и ожидаемый результат в API или БД». Однако в тексте задачи такие описания отсутствуют. Необходимо добавить детальные пошаговые сценарии с указанием: API-эндпоинтов, формата запросов/ответов, триггеров (событие изменения статуса), ролей акторов и ожидаемых записей в БД.; Указано поле next_retry_at, но не определена стратегия расчёта времени повторной попытки (fixed interval, exponential backoff, кастомная логика). Необходимо зафиксировать алгоритм определения next_retry_at в зависимости от номера попытки (attempt_number).; В требовании указана «ссылка на внутренний журнал аудита», но не определён формат этой ссылки (URL-строка, ID записи, GUID, полный путь). Необходимо уточнить тип и формат поля для интеграции.; Требуется batch_id, reason, affected_count для массовых операций, но не определено, какое количество заявок считается «массовой операцией». Необходимо указать порог (например, >1 заявки или >N заявок).; Упомянуты поля delivery_error_code и delivery_error_message, но не определён справочник допустимых кодов ошибок. Необходимо привести перечень возможных кодов или ссылку на справочник.; Не указано максимальное количество попыток повторной отправки или timeout. Это может привести к бесконечному циклу retry. Необходимо определить max_attempts или условие остановки.; Для интеграционных событий описан payload с полями request_id, correlation_id и т.д. Однако для аудитных событий (event_id, entity_type, entity_id...) формат payload не определён — только перечислены поля сохранения. Необходимо указать структуру аудитного события (JSON-схему).; questions: {'text': 'Какой максимальный лимит повторных попыток (max_attempts) предусмотрен перед переходом в финальный статус ошибки?', 'context': 'Модель повторной отправки'}; {'text': 'Требуется ли API-эндпоинт для ручного запуска повторной отправки (manual retry) администратором?', 'context': 'Модель повторной отправки'}; {'text': 'Как именно реализуется требование «вопросы команды о восстановлении после сбоя можно было потом получать в API в validation следующих задач»? Это отдельный эндпоинт для получения истории retry?', 'context': 'Отчётность и API'}; {'text': 'Какова политика хранения данных о неуспешных доставках и истории повторных попыток (TTL в БД)?', 'context': 'Хранение данных'}; {'text': 'Должен ли существовать API для получения текущего статуса доставки конкретного события (delivery_status) по request_id или event_id?', 'context': 'API-требования'}
- 2026-04-26T14:06:05+05:00: `after-strengthening-1` -> `needs_rework`; issues: Не определена стратегия вычисления времени следующей попытки (next_retry_at). Отсутствует описание формулы или алгоритма расчёта интервала retry (exponential backoff, fixed delay и т.д.).; Не указаны граничные значения для механизма retry: максимальное количество попыток, TTL записей, время жизни задач в очереди.; Критерий приёмки 1 требует описания для КАЖДОГО шага: входные данные, актор, ожидаемый результат. В текущей спецификации описаны только общие поля, но не конкретные сценарии использования API.; Не определено, какие именно статусы заявки считаются «ключевыми» для публикации события. Требуется явный перечень или критерий отбора.; Не раскрыт формат и структура API для получения истории повторных отправок («в validation следующих задач»). Отсутствует описание эндпоинтов, параметров запроса и формата ответа.; Не указаны коды ошибок бизнес-валидации CRM, которые должны обрабатываться отдельно от технических ошибок ( недоступность).; Не определён формат отчётности по истории повторных отправок. Требуется описание полей, агрегаций и периодичности.; Требование «идемпотентность определяется парой request_id + correlation_id» не объясняет, как генерируется correlation_id и кто его создаёт.; Не указан таймаут HTTP-запросов к внешней CRM и поведение системы при его превышении.; Не определено, должен ли batch_id быть уникальным идентификатором или допускается повторное использование для логически связанных операций.; questions: {'id': 1, 'text': 'Какова стратегия расчёта next_retry_at: фиксированный интервал, экспоненциальный backoff, или кастомная логика?'}; {'id': 2, 'text': 'Сколько максимально допустимо попыток повторной отправки перед переходом в состояние permanent failure?'}; {'id': 3, 'text': 'Какие конкретно статусы заявки являются «ключевыми» и должны триггерить интеграционное событие?'}; {'id': 4, 'text': 'Какой API и формат ответа должен предоставлять доступ к истории retry для «validation следующих задач»?'}; {'id': 5, 'text': 'Какие коды ошибок CRM считаются «ошибками бизнес-валидации» и требуют специальной обработки?'}; {'id': 6, 'text': 'Какой формат должен иметь отчёт по истории повторных отправок (поля, агрегации, фильтры)?'}; {'id': 7, 'text': 'Какова структура и источник generation correlation_id?'}; {'id': 8, 'text': 'Какой таймаут устанавливается для HTTP-вызовов к CRM?'}; {'id': 9, 'text': 'Как обрабатывается ситуация, когда external_employee_id отсутствует или невалиден в payload?'}; {'id': 10, 'text': 'Какова допустимая задержка между внутренним переходом статуса и публикацией события (синхронно или асинхронно)?'}
- 2026-04-26T14:10:55+05:00: `after-strengthening-2` -> `needs_rework`; issues: Не определено понятие «ключевых изменений статуса» — какие именно статусы или переходы триггируют событие. Требуется явный список статусов или критерий определения ключевых изменений.; Отсутствует логика определения «validation следующих задач» — неясно, что именно подразумевается под получением вопросов о восстановлении после сбоя в API. Требуется описание конкретного endpoint или формата ответа.; Не указаны параметры retry-механизма: максимальное количество попыток, интервал между повторами, алгоритм вычисления next_retry_at. Требуется спецификация backoff-стратегии.; Не определены возможные значения delivery_status и delivery_error_code. Требуется enum или справочник допустимых значений.; Не указано, что происходит при исчерпании попыток повторной отправки (max retries reached). Требуется явное описание финального состояния записи.; Требование «история повторных отправок была доступна в отчётности» не конкретизировано — неясно, какие именно поля/метрики должны быть доступны в отчётах.; Не указано, какие именно бизнес-валидации могут вернуть ошибку от CRM. Требуется перечень или категории ошибок бизнес-валидации.; Не определён критерий недоступности CRM (какой HTTP-код, таймаут, какое количество ошибок считается недоступностью).; В требованиях к аудиту указан actor_user_id, но в payload внешнего события указан external_employee_id — неясно, как они связаны и какой актор инициирует событие.; Требуется уточнение: batch_id генерируется системой или передаётся извне? Для каких массовых операций применимо это требование?; questions: Какие конкретно статусы заявки считаются «ключевыми» для публикации события?; Какой API endpoint предоставляет информацию о восстановлении после сбоя (validation задач)?; Какова максимальное число попыток повторной отправки и формула расчёта next_retry_at?; Какие значения допустимы для delivery_status и delivery_error_code?; Что происходит при достижении лимита повторных попыток — запись переходит в финальный статус?; Какие поля должны быть доступны в отчётах по истории повторных отправок?; Какие конкретно ошибки бизнес-валидации может вернуть внешняя CRM?; Какой код ответа HTTP или таймаут считается признаком недоступности CRM?; Какова связь между external_employee_id в payload и actor_user_id в аудите?; Для каких массовых операций требуется batch_id — только для повторных отправок или для всех?
- 2026-04-26T14:14:35+05:00: `after-strengthening-3` -> `needs_rework`; issues: Не определён формат API для получения статуса доставки и истории повторных отправок. Требование 'восстановление после сбоя можно было получать в API' не содержит спецификации эндпоинтов, методов и формата ответа.; Не задан перечень возможных значений поля 'delivery_status'. Требование 'отдельный статус доставки' подразумевает конечное множество статусов, но они не перечислены.; Не определена стратегия повторных отправок: интервалы между попытками, максимальное количество попыток, формула расчёта 'next_retry_at'. Упоминается только наличие поля, но не логика его заполнения.; Не определено, какие именно изменения статуса считаются 'ключевыми' для публикации события. Требование 'после ключевых изменений статуса' не содержит критерия определения ключевости.; Не задан формат ссылки на внутренний журнал аудита в составе события. Требуется спецификация: URL-формат, идентификатор записи аудита или полный путь.; Не определён перечень кодов ошибок бизнес-валидации CRM ('delivery_error_code'). Отсутствует классификация возможных ошибок.; Требование 'история повторных отправок была доступна в отчётности' не содержит спецификации: формат отчёта, API-эндпоинт, фильтры, периодичность.; Не указан протокол взаимодействия с внешней CRM (REST, SOAP, gRPC, message queue). Требования к формату payload есть, но транспортный уровень не определён.; Не определена политика хранения данных о повторных отправках: срок хранения, архивирование, cleanup.; Критерий приёмки 'проверка выполняется без UI' не содержит конкретных запросов/ответов API для верификации, только абстрактное описание.; questions: Какие статусы заявки считаются 'ключевыми' для триггера интеграционного события? Есть ли перечень или правило?; Какие конкретно значения может принимать delivery_status (pending, delivered, failed, retrying)?; Какова стратегия backoff для next_retry_at (фиксированный интервал, экспоненциальный, настраиваемый)?; Какой протокол используется для интеграции с внешней CRM (HTTP REST, очередь сообщений)?; Какой формат имеет поле 'correlation_id' (GUID, UUID, натуральный ключ)?; Какова максимальная глубина истории повторных отправок для хранения?; Какие именно поля должны быть доступны в API восстановления после сбоя (только последняя попытка или полная история)?; Какой формат отчётности ожидается (CSV, JSON API, дашборд, scheduled report)?; Нужна ли аутентификация/авторизация при обращении к API аудита извне?; Какие действия предусмотрены при достижении максимального числа попыток (остановка, ручное вмешательство, алерт)?

            ## Сохранённые backlog-вопросы

            - QA backlog для этой задачи не пополнялся.

            ## Повторное использование backlog-вопросов

            - Повторно использованных backlog-вопросов не зафиксировано.

            ## Change Proposals

            - Proposal-событий пока нет.

            ## Вложения

            - Вложений нет.

            ## Финальный статус

            - ещё не завершена

            ## Вывод для магистерской

            - Сценарий показал базовый lifecycle задачи и взаимодействие команды.
