# Аудит массовых изменений маршрута согласования

            - Task ID: 620106b6-3cee-4adc-9b63-e91372441432
            - Теги: audit, integration, reports
            - Дата создания: 2026-04-26T09:14:38.250759Z

            ## Исходное описание

            # Аудит массовых изменений маршрута согласования

- Теги: audit, integration, reports

## Стартовая редакция

Нужно описать аудит массовых операций, когда аналитик или администратор меняет маршрут согласования сразу у группы заявок после реорганизации или изменения лимитов. Система должна сохранять, кто выполнил массовое действие, сколько заявок затронуто и какие шаги маршрута были пересобраны.

Требование должно быть пригодно для отчётности и последующего расследования: у каждой массовой операции должен быть единый batch id, а у каждой заявки — ссылка на эту операцию, старый маршрут и новый маршрут. Также важно зафиксировать, как это сочетается с уже отправленными во внешнюю CRM событиями и повторными доставками.

Критерии приёмки:
1. Для массовой операции сохраняются actor, batch id, причина запуска и количество затронутых заявок.
2. По каждой заявке можно увидеть старый и новый маршрут согласования.
3. В отчёте аудита видно, какие интеграционные события были связаны с массовым изменением.

## Редакция после доработки

Нужно описать аудит массовых операций, когда аналитик или администратор меняет маршрут согласования у группы заявок после реорганизации, смены лимитов или обновления матрицы полномочий. Система сохраняет actor, batch id, причину запуска, количество затронутых заявок и время применения операции.

Требование должно отдельно фиксировать следы по каждой заявке: старый маршрут, новый маршрут, активный шаг до изменения, активный шаг после изменения и связь с уже отправленными во внешнюю CRM событиями. Если операция затронула заявку, по которой уже выполняется повторная интеграционная отправка, аудит обязан показать это явно, чтобы аналитик понимал контекст и мог сопоставить сбой интеграции с изменением маршрута.

Критерии приёмки:
1. У каждой массовой операции есть actor, batch id, причина и временная метка.
2. По каждой затронутой заявке видно старый и новый маршрут, а также активный шаг до и после изменения.
3. Отчёт аудита отображает связанные интеграционные события и повторные отправки.

            ## Хронология

            - 2026-04-26T14:14:38+05:00: Создана задача в статусе `draft`.
- 2026-04-26T14:14:38+05:00: Для первого живого прогона в задачу сразу добавлено стандартизированное приложение с критериями.
- 2026-04-26T14:15:50+05:00: Автопроверка вернула `needs_rework`, задача будет усилена и перевалидирована.
- 2026-04-26T14:18:32+05:00: После `after-strengthening` attempt 1 аналитик уточнил критерии и граничные сценарии.
- 2026-04-26T14:19:27+05:00: После `after-strengthening` attempt 2 аналитик уточнил критерии и граничные сценарии.
- 2026-04-26T14:23:54+05:00: После `after-strengthening` attempt 3 аналитик уточнил критерии и граничные сценарии.

            ## Ключевые сообщения чата

            - Пока без сообщений.

            ## Результаты валидации

            - 2026-04-26T14:15:50+05:00: `initial` -> `needs_rework`; issues: Не определены конкретные API-эндпоинты для выполнения массового изменения маршрута. Отсутствует описание HTTP-методов, путей, формата запроса и ответа.; Критерий 'Для каждого шага описаны входные данные, разрешённый актор и ожидаемый результат' не выполнен. Описаны только общие поля аудита, но не конкретные шаги процесса массового изменения и их валидация.; Не описан механизм обработки батча при частичных ошибках. Что происходит, если изменение маршрута для одной заявки из батча не удалось? Откатывается весь батч или продолжается?; Требование 'Если операция затронула заявку, по которой уже выполняется повторная интеграционная отправка, аудит обязан показать это явно' сформулировано, но не определено: как именно это показывается в API/БД, какие дополнительные поля записываются.; Не определены коды ошибок при выполнении массовой операции. Граничные случаи 'перечислены явно' - требование не выполнено.; В разделе 'Интеграционные события' используется 'status', в 'Аудит и отчёты' - 'from_status'/'to_status'. Необходимо уточнить, это статусы заявки или статусы интеграции.; Не описано, как аудит должен обрабатывать повторные изменения маршрута для одной заявки. Сохраняется ли история всех изменений или только последнее?; questions: Какой API-эндпоинт выполняет массовое изменение маршрута? Какие параметры принимает?; Каков максимальный размер batch_id (количество заявок в одной операции)?; Как определяется 'разрешённый актор' для выполнения массовой операции - какая роль или permission?; При частичном сбое батча (например, 95 из 100 заявок обработаны успешно) - какой HTTP-код возвращается и как передаётся информация об ошибках?; Какие именно поля показывают 'повторную интеграционную отправку' в аудите - это отдельный флаг или связь с таблицей интеграций?; Нужно ли фиксировать в аудите изменения, которые не привели к фактическому изменению маршрута (старый == новый)?; Как система обрабатывает изменение маршрута для заявки, которая уже находится на утверждении (есть активный процесс согласования)?
- 2026-04-26T14:18:30+05:00: `after-strengthening-1` -> `needs_rework`; issues: Не определена структура данных для понятий 'старый маршрут' и 'новый маршрут'. Требуется явное описание модели данных: является ли маршрут списком шагов, идентификатором процесса, или иной структурой.; Не определено поле связи аудита с интеграционными событиями. В разделе 'Аудит и отчёты' отсутствует поле для хранения correlation_id или идентификатора интеграционного события, что делает невозможным выполнение требования 'аудит обязан показать это явно'.; Понятие 'активный шаг до/после изменения' не определено. Неясно, является ли это статусом заявки, идентификатором этапа согласования, или иным атрибутом.; Критерий приёмки 'для каждого шага описаны входные данные, разрешённый актор и ожидаемый результат' не выполнен — сами шаги и их входные данные не перечислены.; Требование 'Граничные случаи и ошибки перечислены явно' не выполнено — не указаны сценарии: пустой результат массовой операции, частичный успех, конфликт изменений, превышение лимита batch_size.; Отсутствует спецификация API для выполнения массовой операции: endpoint, метод, формат запроса, пагинация при большом количестве заявок.; Не указано, является ли массовая операция синхронной или асинхронной; как обрабатывается таймаут при большом объёме данных.; Теги 'audit', 'integration', 'reports' не полностью отражают предметную область. Отсутствуют теги, отражающие массовые операции, документооборот или согласование.; questions: Какова структура данных маршрута согласования? Это массив идентификаторов шагов, ссылка на процесс, или иная модель?; Как именно связать аудит-запись с интеграционным событием — через какое поле (correlation_id, event_id, request_id)?; Что означает 'активный шаг' — это текущий статус заявки, индекс этапа в маршруте, или идентификатор задачи?; Какие именно API-методы должны быть реализованы: для запуска массовой операции, для получения статуса batch, для получения деталей аудита?; Каковы ограничения на размер batch (максимальное количество заявок за одну операцию)?; Какие роли могут выполнять массовые изменения — только администратор, или также аналитик с ограниченными правами?; Как система должна вести себя при попытке изменить маршрут у заявки, которая уже находится в процессе обработки (active workflow)?; Требуется ли возможность отката (rollback) массовой операции?
- 2026-04-26T14:19:25+05:00: `after-strengthening-2` -> `needs_rework`; issues: Критерий приёмки требует описания входных данных, разрешённого актора и ожидаемого результата в API для каждого шага, но спецификация содержит только общие поля без конкретных эндпоинтов, методов и форматов запросов/ответов.; Пункт 2 машинных критериев приёмки требует явного перечисления граничных случаев и ошибок, но они не представлены. Необходимо описать: что происходит при пустом batch, при дублирующихся request_id, при timeout внешней системы, при частичном успехе batch-операции.; Scope заявляет 'UI и визуальные экраны не входят в критерии приёмки', но раздел 'Аудит и отчёты' требует генерации отчётов без уточнения, как их проверять без UI. Необходимо либо убрать отчёты из требований, либо явно определить формат проверки (JSON, CSV, файл).; Описание требует фиксировать 'старый маршрут, новый маршрут, активный шаг до изменения, активный шаг после изменения и связь с событиями CRM', но в машиночитаемой спецификации аудита эти поля отсутствуют. Необходимо добавить соответствующие поля в структуру аудита.; Требование 'если операция затронула заявку с повторной интеграционной отправкой, аудит обязан показать это явно' не имеет механизма реализации. Не определено: как система детектирует активную повторную отправку, какое поле в аудите это отражает, как аналитик получает эту информацию.; Не описан механизм массовой операции: как формируется batch_id, как обрабатываются ошибки при применении к отдельным заявкам (частичный успех), как реализуется откат при критических ошибках.; Идемпотентность определяется парой 'request_id + correlation_id', но не указано, где и как это проверяется: в каком сервисе, с каким временем жизни записи, что происходит при дубликате.; questions: Какие конкретные API-эндпоинты должны быть реализованы (GET /audit, POST /batch, GET /batch/{id})?; Какой формат отчётов ожидается (JSON, CSV, XLSX) и как выполняется их проверка без UI?; Каковы граничные значения для 'affected_count' (максимальный размер batch)?; Как система определяет 'активную повторную интеграционную отправку' — по какому статусу или полю?; Должен ли аудит фиксировать все записи batch-операции или только финальный результат?; Какова политика хранения аудита (срок жизни данных)?; Должен ли batch поддерживать откат (rollback) и если да, то какой механика?
- 2026-04-26T14:23:52+05:00: `after-strengthening-3` -> `needs_rework`; issues: Противоречие в Scope: указано 'UI и визуальные экраны не входят в критерии приёмки', но описание задачи содержит 'аналитик понимает контекст' и 'мог сопоставить', что подразумевает наличие интерфейса для просмотра аудита; Не определена структура данных для 'маршрута согласования': в требованиях указаны 'старый маршрут, новый маршрут, активный шаг до/после изменения', но формат хранения (ID, JSON, ссылка на workflow) не описан; Противоречие между разделами: в 'Аудит и отчёты' указаны поля from_status/to_status, но в основном описании речь идёт о маршруте согласования (workflow). Неясно, это одно и то же или разные сущности; Раздел 'Интеграционные события' не связан с основной задачей 'Аудит массовых изменений маршрута'. Неясно: это часть той же массовой операции или отдельный процесс? Как payload интеграции связан с изменением маршрута?; Критерий приёмки #1 требует 'для каждого шага описаны входные данные, разрешённый актор и ожидаемый результат', но в задаче нет явного пошагового описания с такими данными; Критерий приёмки #2 требует 'граничные случаи и ошибки перечислены явно', но в задаче они отсутствуют. Не описан сценарий: частичный успех операции, изменение заявки в процессе обработки, конфликт с параллельными операциями; Требование 'аудит обязан показать это явно, если операция затронула заявку с повторной интеграционной отправкой' не имеет конкретной реализации: в каком поле, каком формате, по какому ключу это отображается?; Отсутствует описание API-эндпоинтов для выполнения массового изменения маршрута и получения аудита. Критерий приёмки требует проверки через API, но сами API не определены; Упоминаются 'аналитик' и 'администратор' как акторы, но не определено: какие роли/права нужны, различаются ли их полномочия, есть ли ограничения на массовые операции; Не описан механизм обработки ошибок внутри массовой операции: откат при частичном успехе, повреждение данных при прерывании, порядок применения изменений; Требование 'связь с уже отправленными во внешнюю CRM событиями' не имеет конкретной реализации: как хранится эта связь, по какому ключу (correlation_id?), формат ссылки; questions: Какова структура данных маршрута согласования (workflow definition, матрица полномочий)?; Чем отличается 'статус' в разделе аудита от 'маршрута согласования' в основном описании?; Как интеграционные события (external payload) связаны с изменением маршрута - это часть той же транзакции или отдельный процесс?; Какие именно шаги с входными данными и ожидаемыми результатами должны быть описаны для критерия приёмки #1?; Какие граничные случаи и ошибки должны быть перечислены (критерий #2)?; Как именно аудит должен показать факт 'повторной интеграционной отправки' - добавить поле в аудит-запись, создать связь, добавить флаг?; Какие API-эндпоинты нужны: для запуска массовой операции, для получения аудита по batch_id, для получения деталей по заявке?; Какие роли и права доступа предусмотрены для аналитика и администратора?; Что происходит при ошибке в середине массовой операции - откат всех изменений или частичный успех?; Как хранится связь с CRM-событиями - по какому ключу (request_id, correlation_id)?; Какова структура таблиц/сущностей для хранения аудита массовых операций?; Предусмотрена ли возможность отмены (undo) массового изменения маршрута?

            ## Сохранённые backlog-вопросы

            - QA backlog для этой задачи не пополнялся.

            ## Повторное использование backlog-вопросов

            - Повторно использованных backlog-вопросов не зафиксировано.

            ## Change Proposals

            - Proposal-событий пока нет.

            ## Вложения

            - Вложений нет.

            ## Финальный статус

            - ещё не завершена

            ## Вывод для магистерской

            - Сценарий показал базовый lifecycle задачи и взаимодействие команды.
